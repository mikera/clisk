(ns clisk.samples.walkthrough
  (:use [clisk core node functions patterns colours textures util]))

(defn walthrough [] 
  
;; colours in RGB  
(show [1 0 0])

;; named colours
(show pink)


(show [x 0 0])

(show [x y 0])

)

